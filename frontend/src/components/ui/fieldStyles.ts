/**
 * 입력 필드 공통 외형 — TextInput·TextArea 가 공유한다.
 * 도메인 코드는 이 문자열 대신 두 컴포넌트를 쓴다.
 */
export const fieldClass =
  'rounded-md border border-border bg-surface-2 px-3 py-2 text-sm text-text-1 outline-none focus:border-accent';
