/**
 * 스테이지 좌우 여백 — 헤더 제목과 콘텐츠(보관함 등)가 같은 세로선에 맞도록 공유한다.
 *
 * 한 곳에서만 정의해 StageHeader 와 콘텐츠 뷰가 같은 값을 쓰게 한다.
 * 성좌·지도처럼 화면을 꽉 채우는 뷰(풀블리드)는 이걸 쓰지 않는다.
 *
 * Tailwind 는 클래스를 정적으로 스캔하므로 이 문자열이 그대로 있어야 유틸이 생성된다.
 */
export const STAGE_PX = 'px-5 desktop:px-[34px]';

/**
 * StageHeader 가 차지하는 높이만큼 콘텐츠를 아래에서 시작시키는 여백.
 *
 * 헤더는 absolute 라 흐름에서 빠져 있어 **아래 콘텐츠가 스스로 자리를 비워야 한다.**
 * 그래서 헤더 높이가 바뀌면 이 값도 같이 바뀌어야 하는데, 값이 각 화면에 흩어져 있으면
 * 반드시 어긋난다 — 실제로 헤더에 안전영역을 더했을 때 이 여백이 따라오지 않아 헤더가
 * 카테고리 칩 바(관리·즐겨찾기)를 덮었다.
 *
 * 안전영역을 포함하는 이유가 그것이다. 헤더가 상태바만큼 내려가면 이 값도 같이 내려간다.
 * STAGE_PX 와 같은 이유로 한 곳에서만 정의한다.
 */
export const STAGE_PT = 'pt-[calc(64px+var(--safe-top))] desktop:pt-[calc(72px+var(--safe-top))]';
