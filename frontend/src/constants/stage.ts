/**
 * 스테이지 좌우 여백 — 헤더 제목과 콘텐츠(보관함 등)가 같은 세로선에 맞도록 공유한다.
 *
 * 한 곳에서만 정의해 StageHeader 와 콘텐츠 뷰가 같은 값을 쓰게 한다.
 * 성좌·지도처럼 화면을 꽉 채우는 뷰(풀블리드)는 이걸 쓰지 않는다.
 *
 * Tailwind 는 클래스를 정적으로 스캔하므로 이 문자열이 그대로 있어야 유틸이 생성된다.
 */
export const STAGE_PX = 'px-5 desktop:px-[34px]';
