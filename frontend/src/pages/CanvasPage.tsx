import StagePlaceholder from '@/components/domain/stage/StagePlaceholder';

/**
 * 한눈에 보기(캔버스) — 보드·툴·코멘트 핀.
 *
 * TODO: 목업 v3.5/canvas-view.html 이식
 * TODO: 데스크톱 전용 라우트 가드 (< lg 에서는 /universe 로 리다이렉트)
 */
const CanvasPage = () => <StagePlaceholder label="한눈에 보기" />;

export default CanvasPage;
