import StagePlaceholder from '@/components/domain/stage/StagePlaceholder';

/**
 * 대시보드(파인더) — 폴더 그리드 → 폴더 진입 → 정사각 아이템.
 * TODO: 목업 v3.5/dashboard.html 이식
 */
const LibraryPage = () => <StagePlaceholder label="대시보드" />;

export default LibraryPage;
