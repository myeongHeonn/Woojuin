import StagePlaceholder from '@/components/domain/stage/StagePlaceholder';

/**
 * 지도 뷰 — 핀 리스트 + 카테고리 칩.
 * TODO: 목업 v3.5/map-view.html 이식 (react-map-gl + MapLibre)
 */
const MapPage = () => <StagePlaceholder label="지도" />;

export default MapPage;
