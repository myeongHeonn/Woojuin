# 빌드 컨텍스트에서 제외할 것들.
# 이미지 안에서 처음부터 빌드하므로 호스트의 빌드 산출물은 넣을 필요가 없고,
# 넣으면 컨텍스트 전송이 느려지고 예전 jar 가 섞여 들어갈 위험도 있다.
build/
.gradle/

# 래퍼는 쓰지 않는다(빌드 스테이지의 gradle:8.8-jdk21 이미지에 gradle 이 들어있음)
gradlew
gradlew.bat
gradle/

# 시크릿·로컬 설정이 이미지에 딸려 들어가지 않게
.env
*.log

.idea/
*.iml
