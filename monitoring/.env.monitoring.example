# 모니터링 스택 환경변수 — 실제 파일(.env.monitoring)은 서버에만 두고 커밋 금지. chmod 600.
#   서버 배치 예: /home/ubuntu/woojuin/monitoring/.env.monitoring

# Grafana 관리자(admin) 비밀번호. 생성 예: openssl rand -base64 18
GRAFANA_ADMIN_PASSWORD=__CHANGE_ME__
